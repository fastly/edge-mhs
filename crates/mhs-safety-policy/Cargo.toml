[package]
name = "mhs-safety-policy"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Schema-driven safety-limit evaluation for MHS device tool calls. No Fastly dependency — runs and tests on a plain host."

[dependencies]
serde.workspace = true
serde_json.workspace = true
